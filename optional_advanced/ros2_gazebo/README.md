# Optional Advanced Track ? ROS 2 and Gazebo

This folder preserves the repository's previous ROS 2 Jazzy/Gazebo material for optional middleware, TF2, `ros2_control`, and MoveIt study. It is separate from the Webots-first Lab 00?12 sequence.

The archived folders retain their original commands and assumptions. They may require updates to match current upstream packages.

| Archived material | Advanced topic |
|---|---|
| `ROOT_README.md` | Original repository overview |
| `lab00_setup/` | ROS 2 Jazzy and Gazebo installation |
| `lab01_ros2_gazebo_ur5e/` | ROS graph, actions, and `ros2_control` |
| `lab02_frames_tf/` | TF2 frame queries |
| `lab12_moveit_collision_planning/` | MoveIt 2 planning scenes |

Complete the primary Webots lab with the same number before attempting an advanced exercise.

These exercises may call framework algorithms because their purpose is middleware comparison. They must not replace the explicit mathematical implementations in the primary labs.
